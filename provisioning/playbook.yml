---
- hosts: all
  tasks:
    - include: common.yml
  handlers:
    - include: handlers.yml

- hosts: publicdb
  tasks:
    - include: mysqldb.yml
    - include: development.yml
    - include: publicdb.yml
    - include: fake_datastore.yml
  handlers:
    - include: handlers.yml
