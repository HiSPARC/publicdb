- name: "Ensure {{ hisparc_path }} directory exists"
  ansible.builtin.file:
    path: "{{ hisparc_path }}"
    state: directory

- name: "Ensure {{ hisparc_path }}/raw directory and subdirectories exist"
  ansible.builtin.file:
    path: "{{ hisparc_path }}/raw/{{ item }}"
    state: directory
    mode: 02775
  with_items:
    - /
    - tmp
    - incoming
    - partial
    - suspicious
